# Remove all existing values for remote.origin.url
git config --unset-all remote.origin.url

# Set a new value for remote.origin.url
git config remote.origin.url "git@github.com:bobbyhiddn/{Name_Of_Project}.git"

